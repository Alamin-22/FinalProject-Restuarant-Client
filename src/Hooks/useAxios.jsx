
const axios = axios.create({
    baseURL: "http://localhost:5000",
    withCredentials: true
})

const useAxios = () => {
    return (
        <div>
            
        </div>
    );
};

export default useAxios;